{
  "name": "quidec-functions",
  "description": "Firebase Cloud Functions for Quidec",
  "version": "1.0.0",
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
